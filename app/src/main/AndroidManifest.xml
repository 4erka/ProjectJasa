<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akujasa.jasacenter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jasa_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".MenuUtamaActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".PesaninformasiActivity" />
        <activity android:name=".PesanformActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PesananMaps"
            android:label="Pilih Lokasi Pemesanan" />
        <activity android:name=".KatalogJasaTambah" />
        <activity android:name=".KatalogJasaActivity" />
        <activity android:name=".KatalogJasaEdit" />
        <activity android:name=".StatusPesananTokoBaruKeterangan" />
        <activity android:name=".StatusPesananTokoProgressKeterangan" />
        <activity android:name=".StatusPesananTokoSelesaiKeterangan" />
        <activity android:name=".RegisterTokoActivity" />
        <activity
            android:name=".PesananTokoMapActivity"
            android:label="@string/title_activity_pesanan_toko_map" />
        <activity
            android:name=".RegisterTokoMap"
            android:label="@string/title_activity_register_toko_map" />
        <activity android:name=".PesananInfo" />
        <activity android:name=".EditActivity" />
        <activity android:name=".PesananInfoCancel" />
        <activity android:name=".PesananInfoSelesai" />
        <activity android:name=".ProfilTokoActivity" />
        <activity android:name=".ProfilTokoEditActivity" />
        <activity
            android:name=".ProfilTokoEditMap"
            android:label="@string/title_activity_profil_toko_edit_map"></activity>
    </application>

</manifest>